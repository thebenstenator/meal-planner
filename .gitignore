# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Env — never commit real secrets. Commit .env.example only.
.env
.env.*
!.env.example

# Editor / OS
.vscode/*
!.vscode/extensions.json
.claude/settings.local.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Test / tooling output
coverage/
playwright-report/
test-results/
.tmp/

# NOTE: src/routeTree.gen.ts is generated by the TanStack Router plugin but is
# committed on purpose so `tsc` typecheck in CI works without a Vite run first.

# Supabase local
supabase/.branches/
supabase/.temp/
.vapid-keys.local.json
