# Normalize line endings: store everything as LF in the repo (CI runs on Linux),
# let Windows working copies check out however they like.
* text=auto eol=lf

# Binary assets — never touch.
*.png binary
*.jpg binary
*.jpeg binary
*.webp binary
*.ico binary
*.woff2 binary
